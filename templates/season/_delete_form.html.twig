<form method="post" action="{{ path('season_delete', {'id': season.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input class="btn btn-dark m-2" type="hidden" name="_method" value="DELETE">
    <input class="btn btn-dark m-2" type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn btn-dark m-2">Supprimé</button>
</form>
